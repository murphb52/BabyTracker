# 096 Onboarding Copy And Animation Tweaks

1. Update the onboarding intro and Quick Log page copy to better match the requested tone and wording.
2. Reduce the onboarding primary footer button height slightly without changing the overall layout or button hierarchy.
3. Remove the Quick Log demo card wiggle animation so only the first-event step uses a button emphasis animation.
4. Soften the first-event button wiggle so it still draws attention without feeling too aggressive.
5. Change the timeline demo fill animation from broad category-group reveals to a deterministic staggered cascade with slight overlap between blocks.
6. Verify the affected onboarding views still build and keep previews aligned with the new copy and behavior.
7. Extract a shared onboarding primary button view so all blue onboarding buttons can be adjusted from one place.

- [x] Complete
