# 097 Onboarding Second Screen Refresh

1. Rework the second onboarding screen so its message leads with support and reassurance rather than only speed.
2. Keep the Quick Log demo, but pair it with concise supporting highlights that communicate easy logging, pattern visibility, and partner sharing.
3. Keep the layout readable on smaller devices and respect the existing onboarding visual language.
4. Verify the updated screen still builds cleanly in the feature package.
5. Slow the Quick Log button entrance and make each button’s icon, text, and background animate in as a single unit.
6. Prevent the refreshed Quick Log button labels from clipping inside the compact button layout.
7. Treat each Quick Log tile as a single animated unit so the icon, text, and background move together during the stagger.

- [x] Complete
