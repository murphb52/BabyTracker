# Liquid Glass follow-up implementation

1. Move root banners into safe-area-aware chrome instead of a top overlay.
2. Simplify card shells on the home, status, sleep, picker, and empty-state screens.
3. Replace the inline event delete prompt with a system confirmation dialog.
4. Add accessibility-aware fallbacks for translucent/material surfaces.
5. Validate the change with an Xcode build.

- [x] Complete
